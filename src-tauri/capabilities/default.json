{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main scanner window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-save"]
}
